version: '3.8'

services:
  rustdb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rustdb-server
    ports:
      - "5432:5432"
    volumes:
      - rustdb-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "5432"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  rustdb-data:
    driver: local
